# Syntecxhub_Student-Management-System
Student Management System is a Python CLI application used to add, update, delete, and view student records. It uses OOP concepts with classes, stores data in CSV/JSON files, ensures unique IDs, and demonstrates file handling and basic validation.
